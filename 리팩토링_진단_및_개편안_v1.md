# 영세사업장 자율점검 — 소스코드 현황 진단 및 리팩토링/부분 재개발 개편안 (v1)

> 작성: 2026-07-07 · 대상: 기 구축 서비스(backend/cgr FastAPI + frontend Next.js 14)
> 기준: 제안요청서 v8 (SFR-001~007, DAR-001~010, 온프레미스·비식별화·재현가능성 제약)
> 성격: **1단계 진단(읽기 전용) + 2단계 개편안 제안** — 승인 전 코드 변경 없음

---

## 0. RFP 핵심 제약 (진단 기준)

| # | 제약 | 출처 |
|---|------|------|
| C1 | 개인정보 포함 원본은 **온프레미스(국가정보자원관리원 대구센터)에서만** 처리. 비식별 텍스트만 외부 상용 LLM API 전달 | 사업안내 ①, SFR-003 |
| C2 | 외부 전송 직전 **2차 검증 필터** 의무 (비식별 게이트웨이) | SFR-003 |
| C3 | 데이터 생명주기: 분석 완료 즉시 원본 삭제 → 비식별 결과만 학습용 보존 → 학습 후 삭제 + **처리 이력 로깅** | 사업안내 ①, SFR-003 |
| C4 | 재현가능성: 계산적 판단=로직계산기(결정론), 위반→**사전 정의 표준 권고안 매핑** (확률적 답변 금지) | 사업안내 ②⑤, SFR-002/004 |
| C5 | OCR은 **온프레미스 VLM** (노동문서 특화 파인튜닝, 외부 전송 없이) | 사업안내 ②④, DAR-001 |
| C6 | LLM 어댑터는 모델 교체 가능한 구조 | SFR-002/007 |
| C7 | 기 구축물은 품질 분석 후 리팩토링/부분 재개발 범위를 협의 | 현황 2, SFR-001/002 |

---

## 1. 현황 진단 리포트

### 1.1 종합 등급표

등급은 **RFP(차기 구축) 기준**. "현 운영(SaaS 배포) 기준" 다른 판단이면 비고에 표기.

| 등급 | 항목 | 근거(파일:라인) | 비고 |
|------|------|----------------|------|
| 🔴 심각 | **테스트 전무 (0%)** — tests/ 없음, 프레임워크 미설치 | 저장소 전체 | 리팩토링 착수 전 안전망 필수 |
| 🔴 심각 | **logging 미사용** — `import logging` 0건, stderr print ~30건뿐 | run.py:43~190 등 | 운영 추적·장애 분석 불가 |
| 🔴 심각 | **Vision OCR 이미지 원본 외부 전송** — base64로 OpenAI에 직접 | parsers/image.py:132 | C1·C5 위반. 현 운영에선 의도적 설계였음 → 차기에 온프렘 VLM으로 **부분 재개발** 대상 |
| 🔴 심각 | **관리자 로그(interaction_log)에 원문 미마스킹 저장** | api/routes/ec.py:385, ws.py, guide.py:974 | 현 운영에선 품질검토용 의도적 설계(관리자 전용+30일 자동삭제). RFP 기준으론 마스킹 저장 전환 |
| 🔴 심각 | **프론트 EC/WS 페이지 복제** — buildRealPages·buildRequirementBoard 등 1,000줄+ 동일 | ec/page.tsx:910·1462 ↔ ws/page.tsx:944·1494 | 수정 시 양쪽 누락 위험 상존 |
| 🟡 보통 | `except Exception` 남용 116건 (silent pass 다수) | review.py 8건, ec.py·ws.py 각 4건 등 | 에러 무시 → 원인 추적 불가 |
| 🟡 보통 | **api/routes ↔ web/admin/store(Streamlit 계열) 결합 9곳** | admin.py:29, review.py:36, track.py:7 등 | 레거시 UI 폐기 시 API 전체 영향 |
| 🟡 보통 | 거대 파일 6개 (다책임) — guide 2,967 · ec/page 2,893 · ws/page 2,501 · ContractFormView 1,149 · MobileReviewApp 990 · ScContractFormView 881줄 | frontend/src | ScContractFormView는 ContractFormView와 ~80% 동일 |
| 🟡 보통 | 업로드 MIME 실검증 없음 (확장자 화이트리스트만) | upload_tracker.py:40 | 크기 20MB·확장자·UUID 저장은 양호 |
| 🟡 보통 | reviewStore 메모리↔sessionStorage 동기화 규칙 불명확 / WS가 EC 타입 차용 | lib/reviewStore.ts, ws/page.tsx:25 | |
| 🟢 경미 | verdict 이중화 — 루트(취업규칙 5버킷) vs ec/(3버킷) | verdict.py ↔ ec/verdict.py | **의도적 상이 체계** — 통합 불가, 문서화만 |
| 🟢 경미 | 인라인 스타일 18곳 / 라우트 의미 혼동(`/review/[id]/ec/review`) | guide/page.tsx 등 | |
| 🟢 경미 | WR 결과 화면이 `/review/[id]/page.tsx`(범용)에 내장 | app/review/[id]/page.tsx | 동작엔 문제없음 — 대칭성 위해 wr/로 추출 권장 |

### 1.2 양호 지표 (유지할 강점)

- **PII 마스킹**: 외부 LLM 호출 13곳 중 12곳 마스킹 적용 (EC·WS·SC·guide 전부) — 유일한 예외가 Vision OCR
- **시크릿**: 하드코딩 0건, 전부 env (config.py)
- **임시파일**: 파싱 후 unlink 100% / 업로드 보관기간(기본 30일) 자동삭제 동작
- **계층**: services→routes 역참조 0건, 문서유형 간(ec↔ws↔sc) 상호 import 0건, 죽은 코드 없음
- **결정성 자산 (C4 이미 충족)**: 룰 엔진 V001~V010(결정적 계산+위반메타·권장문안 매핑) = RFP의 "로직계산기+표준 권고안 매핑" 그 자체. temp=0 + llm_cache
- **프론트**: BFF 키 보호(브라우저에 키 노출 0), 의존성 최소(next/react만), CSS Modules 일관

---

## 2. 목표 폴더구조 (개편안)

원칙: **관심사 계층 분리** + **개인정보 경계의 물리적 분리(C1·C2)** + **어댑터 패턴(C5·C6)**. 기존 강점(문서유형 독립, 결정적 엔진)은 위치만 옮기고 구조는 보존.

### 2.1 백엔드

```
backend/cgr/
├── api/                        # [Presentation] FastAPI — 현행 유지
│   ├── main.py · auth.py · jobs.py
│   └── routes/                 # admin · ec · ws · sc · review(wr) · guide · …
├── common/                     # [Common] config · datadir · models · logging(신설)
├── pipeline/                   # [Pipeline] 문서 → 텍스트 → 비식별
│   ├── parsers/                # (이동: cgr/parsers) docx·hwp·hwpx·pdf·plain
│   ├── ocr/                    # (신설) OCR 어댑터 — C5 대비
│   │   ├── base.py             #   OcrEngine 인터페이스
│   │   ├── openai_vision.py    #   현행 외부 Vision (이행기 한정 명시)
│   │   └── onprem_vlm.py       #   온프레미스 VLM 슬롯 (차기 구현)
│   └── privacy/                # (신설·부분 재개발) 개인정보 경계 — C1·C2·C3
│       ├── detector.py         #   검출 엔진 (pii_mask.py 승격, 정규식+NER 하이브리드 확장점)
│       ├── gateway.py          #   외부 전송 게이트웨이 + 2차 검증 필터 (SFR-003)
│       └── lifecycle.py        #   원본삭제→비식별보존→학습후삭제 상태 전이 + 이력 로깅
├── engine/                     # [Service] 판정 — 결정적 (C4)
│   ├── wr/                     # 취업규칙: run·rules·verdict(5버킷)·extractor·prefilter·wr_classify
│   ├── ec/ · ws/ · sc/         # 문서유형별 서비스 (현 내부 구조 그대로 이동)
│   └── calculators/            # 로직계산기 — rule_engine의 계산 룰 승격 (최저임금·가산·주휴·퇴직금)
├── knowledge/                  # [Repository] 기준·참조 데이터 (DAR-003)
│   ├── master_db.py · catalog.py · topic_db.py · legal_links.py · law_category.py
│   └── recommendations/        # 위반유형 ↔ 표준 권고안 매핑
├── llm/                        # [Adapter] LLM 연동 — C6
│   ├── client.py               # 모델 교체형 인터페이스 (외부 상용 ↔ 온프렘 오픈소스)
│   ├── prompt_store.py · cache.py (이동: llm_cache)
├── store/                      # [Repository] 운영 데이터 — web/admin/store 분리·승격
│   ├── analytics.py · history.py · access_log.py · settings_store.py · slot_writer.py
└── web/                        # [레거시] Streamlit — store 의존 제거 후 격리 → 단계적 폐기
```

**호출 방향 규칙(단방향)**: `api → engine/pipeline → knowledge/store/llm → common`.
- privacy/gateway 를 거치지 않은 데이터는 llm/ 로 진입 금지 (import 수준에서 강제: llm.client 는 gateway 통과 토큰/마킹 없는 raw 입력 거부)
- engine 내 문서유형 간 직접 import 금지 (현행 준수 상태 유지)
- store(운영 데이터)와 knowledge(참조 데이터)는 서로 모름

### 2.2 프론트엔드

```
frontend/src/
├── app/                        # 라우트 — 페이지는 조립만
├── features/review/            # (신설) 문서유형 공용 결과화면 로직
│   ├── constants.ts            # APPROPRIATENESS_ORDER · VERDICT_STYLES (현재 EC/WS 중복)
│   ├── pageBuilders.tsx        # buildRealPages · buildRequirementBoard 공용화
│   └── forms/GenericFormView.tsx  # EC·SC 양식 통합 (필드 스키마 주입형)
├── components/ · lib/ · data/ · types/   # 현행 유지 (인라인 스타일만 CSS로)
```

---

## 3. 개발표준 (요약)

| 영역 | 표준 |
|------|------|
| 계층 | 위 단방향 호출 규칙. routes 는 얇게(파라미터 검증+서비스 호출), 비즈니스 로직은 engine/ |
| 명명 | 패키지=소문자 단수, 서비스 함수=동사(analyze·generate), 라우트=리소스 명사. 프론트 컴포넌트=PascalCase, 훅=use* |
| 예외 | 도메인 예외 클래스(CgrError 계열) + api/main 전역 핸들러 1곳. `except Exception` 은 최외곽·기회적 정리에만, 반드시 log 동반 |
| 로깅 | logging 모듈 표준화(JSON 포맷 옵션), print 금지. **로그에 PII 금지**(마스킹 후 기록) — C3 이력 로깅과 통합 |
| 설정 | 모든 시크릿·엔드포인트 env 외부화(현행 준수). 기본값에 시크릿 금지. SFR-007 대비 공통코드·API설정은 store/settings 로 |
| 결정성 | 판정 변경은 ruleset_version 갱신 필수. LLM 호출은 temp=0+캐시. 캐시 키에 프롬프트 버전 포함 |
| 테스트 | 판정 엔진=골든(스냅샷) 테스트(결정성이 있으므로 같은 입력→같은 판정을 그대로 자산화), 라우트=happy+error path |

---

## 4. 리팩토링 / 부분 재개발 범위표

| # | 대상 | 방식 | 근거 | 리스크 |
|---|------|------|------|--------|
| R1 | logging 도입 + except 116건 정리 | 리팩토링 | 심각(추적 불가) | 낮음 |
| R2 | interaction_log 마스킹 저장 + MIME 실검증 | 리팩토링 | RFP C1/입력검증 | 낮음 (품질검토 열람성은 uploads 원본으로 유지) |
| R3 | web/admin/store → cgr/store 분리 | 리팩토링 | 계층 위반 9곳 | Streamlit 경로 수정 필요 |
| R4 | LLM 클라이언트 어댑터화 (llm/client) | 리팩토링 | C6 | 캐시 키 불변 유지 필요 |
| R5 | privacy/ 신설 — 검출엔진 승격 + **2차 검증 필터 게이트웨이** + lifecycle 명시화 | **부분 재개발** | C1·C2·C3 (SFR-003) | 검출률 목표치 별도 협의 |
| R6 | OCR 어댑터 분리 (openai_vision ↔ onprem_vlm 슬롯) | **부분 재개발** | C5 (SFR-002, DAR-001) | 온프렘 VLM 성능은 파인튜닝 사업 결과 의존 |
| R7 | 패키지 재배치 (pipeline/engine/knowledge) — 기능 불변, import 경로만 | 리팩토링 | 관심사 분리 | 리그레션 → R0 골든 테스트로 방어 |
| R8 | 프론트 공용화: 상수→pageBuilders→GenericFormView | 리팩토링 | 중복 1,500줄+ | UI 리그레션 → 화면별 스냅샷 |
| R9 | 거대 파일 분할 (guide·ec/ws page·MobileReviewApp) | 리팩토링 | 유지보수성 | 낮음 (기계적 분리) |
| — | verdict 이중화 | **유지** (문서화만) | 의도적 상이 체계 | — |
| — | 산재위험 진단·SFR-005~007 확장·VLM 파인튜닝 | **신규 개발** (본 개편 범위 밖) | RFP 신규 | 구조만 대비(ocr/·store/settings) |

---

## 5. 단계별 이행 계획

리그레션 금지 원칙: 각 단계는 작은 커밋, 매 커밋 골든 테스트+`tsc`+빌드 통과 확인.

| 단계 | 내용 | 산출 |
|------|------|------|
| **P0 안전망** | pytest 도입 → 판정 골든 테스트(문서유형별 대표 입력 → 판정 JSON 스냅샷 고정) | 리그레션 감지 체계 |
| **P1 관측·보안** | R1(logging/except) + R2(로그 마스킹·MIME) | 운영 추적 가능 상태 |
| **P2 경계 확립** | R3(store 분리) + R4(LLM 어댑터) + R5(privacy 게이트웨이·lifecycle) | C1·C2·C3·C6 구조 충족 |
| **P3 구조 이동** | R7(pipeline/engine/knowledge 재배치) + R6(OCR 어댑터) | 목표 폴더구조 완성 |
| **P4 프론트** | R8(공용화) → R9(분할) | 중복 제거·감량 ~20% |

---

## 6. 승인 요청

위 개편안(§2 목표 구조, §4 범위표, §5 이행 순서)을 승인하는가?
- 전체 승인 / P0~P2만 우선 / 수정 요청 — 중 택일. 승인 전에는 어떤 코드도 변경하지 않는다.
