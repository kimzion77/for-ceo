# 백엔드 FastAPI 주소 — 서버측 fetch + (선택) next.config rewrites 에서 사용
NEXT_PUBLIC_API_BASE=http://localhost:8503

# 인증 키 — server-only (BFF 가 X-API-Key 헤더에 주입). 클라이언트 절대 노출 X
CGR_API_KEY=
# 관리자 엔드포인트용 (PUT/DELETE 류)
CGR_ADMIN_API_KEY=
