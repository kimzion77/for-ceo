# 취업규칙 검토 AI — 백엔드 API 전용 슬림 의존성
#
# 이 파일은 Render/Railway/Fly.io 등 Linux 컨테이너 환경에 FastAPI 서버만
# 배포할 때 사용. Streamlit·pyhwpx(Windows COM)·plotly 등 API 서버에 불필요한
# 의존성은 제외해 빌드 시간과 메모리 사용을 줄인다.
#
# 로컬 풀스택(Streamlit 어드민 포함) 개발은 requirements.txt 사용.

# ─── LLM·검증 ───────────────────────────────
openai>=1.30
pydantic>=2.5
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31         # cgr.embedding 이 OpenAI embedding 엔드포인트 직접 호출용으로 startup 시 import

# ─── 파일 파싱 (FastAPI 가 받는 업로드용) ─────
openpyxl>=3.1
python-docx>=1.1        # .docx 파서
pdfplumber>=0.11        # .pdf 파서 — cgr.parsers.pdf 가 lazy import (필수!)
Pillow>=10.0            # .tif/.bmp → PNG 변환 (cgr.parsers.image). png/jpg 는 불요하나 안전상 포함
olefile>=0.46           # HWP(OLE) 파서 — cgr.parsers.hwp 가 startup 시 import
# pyhwpx 는 Windows COM 라이브러리라 Linux 컨테이너에서 빌드 실패. HWP 파싱은
# olefile + zlib 으로 자체 구현되어 있어 pyhwpx 없이도 정상 동작.
# pypdfium2 는 코드가 실제로 쓰지 않음(pdf.py 는 pdfplumber 사용) — 제외.

# ─── FastAPI 서버 ────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.30
python-multipart>=0.0.9
