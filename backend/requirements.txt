# 취업규칙 검토 AI — 의존성

# 핵심 ────────────────────────────────────
openai>=1.30
pydantic>=2.5
pyyaml>=6.0
python-dotenv>=1.0

# 파일 파싱 ──────────────────────────────────
openpyxl>=3.1
python-docx>=1.1
pyhwpx>=0.45
pypdfium2>=4.30

# 웹 UI (검토 앱 + 관리자 대시보드) ──────
streamlit>=1.32

# REST API 백엔드 ──────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# 관리자 대시보드 — 데이터·차트 ─────────
pandas>=2.0
plotly>=5.20
networkx>=3.2

# (선택) 임베딩 로컬 폴백 ─────────────────
# sentence-transformers>=2.7
